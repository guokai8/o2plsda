% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.sparse_plsda}
\alias{plot.sparse_plsda}
\title{Plot method for Sparse PLS-DA results}
\usage{
\method{plot}{sparse_plsda}(
  x,
  type = "score",
  component = c(1, 2),
  group = NULL,
  top = 20,
  color = NULL,
  ellipse = TRUE,
  ...
)
}
\arguments{
\item{x}{A sparse_plsda object (corrected class name)}

\item{type}{Character. Type of plot: "score", "loading", "vip", "selection"}

\item{component}{Integer or vector. Which component(s) to plot}

\item{group}{Factor. Grouping variable (usually the Y classes)}

\item{top}{Integer. Number of top variables to display}

\item{color}{Custom colors for groups}

\item{ellipse}{Logical. Add confidence ellipses to score plots}

\item{...}{Additional plotting parameters}
}
\description{
Plot method for Sparse PLS-DA results
}
\examples{
# Generate example data
set.seed(123)
n <- 100
p <- 150

X <- matrix(rnorm(n * p), n, p)
# Add class-specific signals
classes <- factor(rep(c("A", "B", "C"), length.out = n))
X[classes == "A", 1:20] <- X[classes == "A", 1:20] + 1.5
X[classes == "B", 21:40] <- X[classes == "B", 21:40] + 1.5
X[classes == "C", 41:60] <- X[classes == "C", 41:60] + 1.5

colnames(X) <- paste0("Gene_", 1:p)

# Fit sparse PLS-DA (using corrected class name)
sparse_plsda_fit <- sparse_plsda(X, classes, nc = 2, keepX = c(30, 25))
class(sparse_plsda_fit) <- "sparse_plsda"  # Ensure correct class

# Score plot
plot(sparse_plsda_fit, type = "score", group = classes, ellipse = TRUE)

# Loading plot
plot(sparse_plsda_fit, type = "loading", component = 1, top = 20)

# VIP plot
plot(sparse_plsda_fit, type = "vip", component = 1, top = 25)

}
